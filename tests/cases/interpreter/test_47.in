// This program uses a random boolean to decide which branch to execute,
// and then prints the appropriate string
if (false) print "if branch"; else if (false) print "else-if branch";
// This program uses a random boolean to decide which branch to execute,
// and then prints the appropriate string
if (false) {
  print "world";
} else if (false) print "world";
if (false) print "world"; else if (false) {
  print "world";
}
// This program uses multiple if statements to categorize a person
// into different life stages based on their age
var age = 37;
var stage = "unknown";
if (age < 18) { stage = "child"; }
else if (age >= 18) { stage = "adult"; }
else if (age >= 65) { stage = "senior"; }
else if (age >= 100) { stage = "centenarian"; }
print stage;
